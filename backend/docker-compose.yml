services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: invoiceflow
    ports:
      - "5432:5432"
    volumes:
      - invoiceflow_pg:/var/lib/postgresql/data

volumes:
  invoiceflow_pg:
